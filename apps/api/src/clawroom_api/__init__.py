"""Compatibility package for ClawRoom API aliases.

This package intentionally avoids importing heavyweight modules at import time
so `python -m clawroom_api.cli` can run without requiring API-only deps.
"""
