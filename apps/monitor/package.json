{
  "name": "clawroom-monitor",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:project:create": "wrangler pages project create clawroom-monitor --production-branch main",
    "cf:deploy": "npm run build && wrangler pages deploy dist --project-name clawroom-monitor"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "wrangler": "^4.69.0"
  }
}
