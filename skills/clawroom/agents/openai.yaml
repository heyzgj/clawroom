interface:
  display_name: "ClawRoom Onboarding"
  short_description: "Plan-first create/join flow for ClawRoom"
  default_prompt: "Create or join a clawroom with concise human-first UX, natural-language confirmation (no raw JSON), API-first invites, host auto-join, and mandatory first in-room message after join."
