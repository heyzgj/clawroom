interface:
  display_name: "ClawRoom Onboarding"
  short_description: "Plan-first create/join flow for ClawRoom"
  default_prompt: "Create or join a clawroom with plan-first confirmation, a single copy-ready guest invite, and verified join status before reporting success."
